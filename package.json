{
  "name": "aisa-community",
  "version": "1.0.0",
  "private": true,
  "description": "Community showcase and skill registry for AIsa — validation and README tooling.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build:readme": "node scripts/build-readme.mjs",
    "check:readme": "node scripts/build-readme.mjs --check",
    "test": "npm run validate && npm run check:readme"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  }
}
